本代码目的是初次使用混合编程
    1、一定注意，标号在nasm汇编中仅仅只给编译器使用，编译后的目标文件中在对应指令中已经被替换成地址数，
    如果有其他文件需要使用该目标文件中从某个标号开始的代码，那么该目标文件应该使用global关键字进行导出，
    这样外部文件才能够使用extern引入这个标号。

    2、其中C语言关键字extern作用是导入/出函数符号引用，nasm汇编语法中extern同样是为了导入外部文件的
    符号引用，global作用则是导出符号供外部引用。